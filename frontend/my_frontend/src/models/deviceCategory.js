// Model of Device that match with the API's Model

class deviceCategory {
  constructor(name = 'Sample name', data_type = 'Sample data_type') {
    this.name = name
    this.data_type = data_type
  }
}

export default deviceCategory
